# Setup Guide

## Prerequisites
- Python 3.10+
- pip

## Installation
1. Clone the repository.
2. Run `pip install -r requirements.txt`.
3. Copy `.env.example` to `.env` and set variables.

## Environment Variables
- `API_KEY`: Your weather data provider API key.

## Running Tests
Run `pytest tests/` from the project root.
